/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var map = function(arr, fn) {
    
   const newa = [];
    for(var i=0;i<arr.length;i++)
    {
        newa[i] = fn(arr[i],i);
    }

    return newa;
};
